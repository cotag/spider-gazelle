module SpiderGazelle
	#
	# This is an instance of the reactor loop for processing application requests
	# It connects to the Spider to obtain the socket descriptor so it can start accepting requests
	# The Spider may be the same process or a parent processes depending on configuration
	#
	class Gazelle
		def initialize(app, options)
			
		end

		def run
			
		end

		def stop
			
		end


		protected

	end
end
